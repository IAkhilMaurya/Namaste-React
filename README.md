# ---------------------------------------package.json-------------------------------------------------



# Parcel : Its actually a bundler, like a webpack that takes all your files like js/css and images and combine them in a smaller no of files so that the files loads quickly.

# we use npx to install react for our newly app and use npm for the installed react app or if we want to install any manager in our previously installed app.

# Pollyfils : A code which is a replacement for a new version of code.

# Browserslist :  When we write browserslist it means that there is a replacement code for these functionalities which is compatible with newer versions of code.

# It can be said as our neweer versions of js is compatible with older versions of JS.

# Tree Shaking : It is used to remove all the unwanted codes, like if we have 10 lines but we only need 2 lines the it will use 2 and leave/remove the rest.

# npx : It is used for executing.

# npm :  it is used for installing things in your project.

# npx == npm run.

# npm run build : it is used to build our project.

# render means updating something in the DOM.

# JSX : HTML like syntax inside JS code is known as JSX.
# There is  : {} In {}, if we write any code then it will work fine. we can perform any calculations inside {} these things.


# --------------------------React Component--------------------------------

# 1. Functional Component : New way of writing code. :
# Name starts with capital letters, its is normal convention & its not mandatory to do.


# 2. Class Based Component : Older way of writing code. :


